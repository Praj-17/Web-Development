// const simple = module.require('./Module_Second.js')

simple()


// The point to notice is that we cannot use the term .require in a file with an extension 'mjs' hence we go for the second way of importing the module called as ESX(Elastic Sky X) import method

import simple from './Module_Second.mjs'